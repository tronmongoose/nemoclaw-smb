"""skills/dynamic_pricing_skill.py -- Dynamic pricing skill stub.

Uses Nemotron Ultra (config/model_routing.py: task "dynamic_pricing") to
recommend nightly rate adjustments based on occupancy, seasonality, and
comparable listings. Full implementation ships in a later wave.

Placeholder exports (not yet implemented):
    recommend(property_id, target_month) -> dict
"""
from __future__ import annotations


def recommend(property_id: str, target_month: str) -> dict:
    """Return pricing recommendation dict -- stub. Full logic ships in a later wave."""
    raise NotImplementedError("Dynamic pricing implementation pending (later wave)")
