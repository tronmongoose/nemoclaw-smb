# Autonomous hardening pass — 2026-06-19

Ran while you were away, against the approved v4 plan. Scope was deliberately low-regret:
harden, verify, finish loose ends, clean up, apply only the design direction you already
endorsed. No new subjective product/design bets. Your real STR data under
`~/nemoclaw-tenants/` was never read or touched (frontier firewall held).

## What changed (6 workstreams, 7 commits)

1. **Docs honesty** (`ab6c5a5`) — the reality matrix in CLAUDE.md / ANALYSIS.md / README.md now
   states each integration's **default** state and its **keyed/enabled** upgrade separately
   (the status columns were reading as overclaims). C1 row corrected to LIVE-OK. Each row notes
   whether a `tests/live` falsifying test exists; Intuit flagged "no falsifying test — unverified."
2. **UI render-test safety net** (`7f0d400`) — the gap that let a TS-clean build ship a runtime
   crash. Added Vitest + React Testing Library; **48 render tests** across every API-consuming
   component, each with realistic + empty/null fixtures, asserting no object-as-child and no
   throw. The ApprovalQueue test reproduces the exact bug that shipped. Deleted 4 dead components
   orphaned by the dashboard redesign.
3. **Correctness bug fixes** (`9dcd314`) — each with a test: Plaid `$0` no longer counted as an
   expense; revenue-ratio no longer risks inf/NaN on a zero-revenue month; 402 escalation always
   records a string reason in the audit; cashflow **year inferred from the `Cashflows NN` token**
   (robust to filenames with multiple 2-digit numbers) + per-file override — this unblocks the
   multi-year run you wanted. (The pnl `by_category` sign was verified intentional, not a bug.)
4. **Per-tenant config** (`2964b4f`) — spend threshold and policy source are now config-driven
   (env / kwarg) with today's values as fallback, a step toward deploy-for-any-client. Fixed the
   synthetic sample double-counting (it was ingesting two sources; added an include-glob).
5. **Conservative demo polish** (`f5ca2bf`) — the Ops dashboard now leads with one **headline
   callout band** (anomaly + pending approval + annual savings, one amber accent, high contrast),
   derived from existing endpoints. The demo's scene 6 prints the audit chain entry-by-entry so
   the tamper-evidence is visible on camera.
6. **Verify + report** (this file).

## Verification (all green)

- Python offline: **415 passed**, 1 skipped, 8 deselected.
- UI: `npm run build` clean (TS), **48 render tests pass**.
- `make demo`: runs end to end, audit chain tamper-evident: **True**.
- `.env` never committed; working tree clean.

## Live reality matrix (`make reality`, default env — no Stripe key in `.env`)

```
Hermes            LIVE-OK
Nemotron          LIVE-OK
Stripe-SDK        MOCK     (LIVE-OK when STRIPE_SECRET_KEY=sk_test_ is in .env)
Stripe-MCP        MOCK     (needs npx + key + the 3.11 venv)
Baton             LIVE-OK  (binary; access DATA still a fixture until a connector cred)
C1-Policy         LIVE-OK  (graceful fallback; live verify needs a real tenant)
GBrain            MOCK     (in-memory; real MCP client wired, needs gbrain+bun install)
Audit-Chain       REAL
Intuit-QB         MOCK     (needs sandbox creds; no falsifying test)
NeMo-Guardrails   DENYLIST (REAL LLMRails when NEMOCLAW_GUARDRAILS=1)
Sandbox           SUBPROCESS
```
Run `make reality` yourself — it is the source of truth, not this snapshot.

## Deliberately NOT done (needs you)

- **Dual-source merge / vendor aliases** (Chase "Property Manager Co" vs cashflow "Mgmt Fees")
  — changes your numbers, a product decision.
- **New savings/fee narrative + AWS/Figma alternatives** — product/messaging.
- **Depreciation / accrual modeling** — accounting modeling decision.
- **Multi-tenant UI selector + tenant-aware frontier routing** — bigger architecture.

## Your two open items

1. **Multi-year STR longitudinal:** drop your `Oside Cashflows 23` and `24` tabs as CSV into
   `~/nemoclaw-tenants/oceanside-str/data/`. Year-per-file inference now handles them; tell me
   and I'll re-run + confirm the 3-year trend.
2. **The supplement** (`supplement.csv`): mortgage **interest** (not full payment), property tax,
   insurance — fill the amounts and re-run for the true net.
